#ifndef VALVERELATION_H
#define VALVERELATION_H

#if defined(ARDUINO) && ARDUINO >= 100
  #include "Arduino.h"
#else
  #include "WProgram.h"
#endif

class valveRelation {

  public:
    valveRelation();
    
  private:
  
};

#endif
